import types from './types'


const getAll = (data) => {
  return {
    type: types.GET_ALL,
    data
  }
};

export const getPeople = () => {
  return (dispatch) => {
  }
};



export const actionCreators = {
  getAll: () => {
    return (dispatch) => {
    }
  },
};
